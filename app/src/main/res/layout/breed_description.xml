<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/breedDescriptionBottomSheet"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorBackground"
    app:behavior_hideable="true"
    app:layout_behavior="android.support.design.widget.BottomSheetBehavior"
    app:behavior_peekHeight="0dp">


    <ImageButton
            android:id="@+id/breedDescriptionCloseButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:srcCompat="@drawable/ic_close"
            android:tint="?attr/colorAccent"
            android:background="@null"
            android:layout_alignParentStart="true"
            android:layout_margin="8dp"
            android:layout_alignParentTop="true"
            android:contentDescription="@string/close_button" />

    <android.support.v4.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_below="@id/breedDescriptionCloseButton"
        android:layout_height="wrap_content">

    <FrameLayout
        android:id="@+id/breedDescriptionFrameLayout"
        android:layout_width="match_parent"
        android:background="?attr/colorBackground"
        android:layout_height="wrap_content">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:text="@string/error"
            android:textSize="30sp"
            android:visibility="gone" />

        <ProgressBar
            android:id="@android:id/progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />

        <com.example.epamandroid.mvp.views.compoundviews.BreedDescriptionView
            android:id="@+id/breedDescription"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </FrameLayout>

    </android.support.v4.widget.NestedScrollView>

</RelativeLayout>
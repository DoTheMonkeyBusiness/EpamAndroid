<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorBackground"
    tools:context=".views.MainActivity">

    <include
        android:id="@+id/activityMainCustomActionBarLayout"
        layout="@layout/home_action_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/activityMainRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/activityMainCustomActionBarLayout"
        android:layout_above="@id/activityMainBottomNavigationView"
        />

    <!--<FrameLayout-->
        <!--android:id="@+id/activityMainFrameLayout"-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="wrap_content"-->
        <!--android:layout_above="@id/activityMainBottomNavigationView"-->
        <!--android:layout_alignParentTop="true" />-->

    <include
        android:id="@+id/activityMainBottomNavigationView"
        layout="@layout/bottom_navigation_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true" />

</RelativeLayout>